# Copy to .env and fill in.

# A Postgres connection string (Prisma Postgres, Supabase, Neon, Railway,
# or any standard Postgres all work — see README).
DATABASE_URL=""

# Signs session cookies. Generate with: openssl rand -base64 32
SESSION_SECRET=""

# Optional: MSG91 for real OTP delivery. Leave unset to keep dev-mode OTP
# (code is returned in the API response, no SMS sent). See README.
MSG91_AUTH_KEY=""
MSG91_OTP_TEMPLATE_ID=""
MSG91_COUNTRY_CODE="91"
